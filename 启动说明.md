# SylvaPlan · 村小丫启动说明

在线版本：[https://sylvaplan.vercel.app](https://sylvaplan.vercel.app)

使用 Node.js 24.x，在本文件所在目录运行：

```sh
npm run demo
```

打开 http://127.0.0.1:4173 。此模式无需安装依赖或连接数据库。

- **中 / EN** 切换中文与英文，保留需求和选择。
- 收录 1,399 条文旅部公开村名录，原有 9 个样例目的地及其配套单独标注。
- 可搜索、收藏、规划、保存和导出；数据保存在当前浏览器及域名下。
- 每日安排由规则整理；在服务端 `.env` 中配置 `DEEPSEEK_API_KEY` 并重启后，小丫聊天与旅行建议会调用真实 DeepSeek。无密钥或调用失败时仍可使用规则结果。

已有 `.env` 时直接编辑；首次配置可复制 `.env.example`。不要把实际密钥提交到 GitHub。照片为氛围示意，公开名录不等于营业、交通、商家与价格已核实。

原数据库模式请按 README.md 配置 MySQL，并执行 `npm ci` 与 `npm start`。线上 Vercel 版本无需 MySQL，真实生产数据库联调及浏览器截图验收仍未完成。已有测试记录见 VALIDATION.md。

GitHub 保存当前版本源码；Vercel 的自动 Git 部署尚未连接，更新线上版本需另行部署。
